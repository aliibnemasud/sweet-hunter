import React from 'react';

const Sidebar = (props) => {
    console.log(props)

    return (
        <div>            
            <button>Select The lucky One</button>          
        </div>
    );
};

export default Sidebar;